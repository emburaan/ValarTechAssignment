<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.valartech">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.valartech.MVVMApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_popcorn"
        android:roundIcon="@drawable/ic_popcorn"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:usesCleartextTraffic="true"
        android:resizeableActivity="false"
        android:networkSecurityConfig="@xml/network_security"
        android:supportsRtl="true"
        android:required="false"
        android:theme="@style/AppThemeStatusBarRed">
        <activity
            android:name="com.valartech.ui.home.HomeActivity"
            android:launchMode="singleTop"
            android:theme="@style/SplashTheme"
            android:windowSoftInputMode="adjustPan|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>